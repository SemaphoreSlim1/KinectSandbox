<UserControl x:Class="KinectSandbox.ColorPicker.View.IndividualColorPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Label Margin="5"  Grid.Row="0" Content="{Binding LayerName, FallbackValue='Layer x'}" />
        <StackPanel Margin="5"  Grid.Row="1" Orientation="Horizontal">
            <TextBlock Text="Min Value" />
            <TextBox Text="{Binding MinValue}" Width="100"/>
        </StackPanel>
        <StackPanel Margin="5"  Grid.Row="2" Orientation="Horizontal">
            <TextBlock Text="Max Value" />
            <TextBox Text="{Binding MaxValue}" Width="100" />
        </StackPanel>
        <xctk:ColorPicker Grid.Row="3" SelectedColor="{Binding SelectedColor}" />
    </Grid>
</UserControl>
