<Controls:MetroWindow x:Class="KinectSandbox.View.ShellView"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                      xmlns:prism="http://www.codeplex.com/prism"
                      Title="MainWindow"
                      ResizeMode="CanResizeWithGrip"
                      Height="350" 
                      Width="525">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".75*"/>
            <ColumnDefinition Width=".25*" />
        </Grid.ColumnDefinitions>
        <ContentControl Margin="10" Grid.Column="0"  prism:RegionManager.RegionName="{Binding Source={StaticResource KnownRegions}, Path=KinectPreview}" />
        <ContentControl Margin="10" Grid.Column="1"  prism:RegionManager.RegionName="{Binding Source={StaticResource KnownRegions}, Path=ColorPicker}" />
    </Grid>
</Controls:MetroWindow>